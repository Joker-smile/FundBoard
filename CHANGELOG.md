# 更新日志

## 2026-09-23

### 🐛 Bug 修复

1. **搜索框回调重复注册**（`gui/toolbar.py`）
   - `__init__` 与 `_setup_bindings` 中各注册了一次 `trace_add`，导致每输入一个字符都会触发两次全表刷新。
   - 现只保留 `_setup_bindings` 中的一处注册。

2. **设置界面的网络参数完全不生效**（`gui/dialogs.py` + `gui/app.py`）
   - 设置对话框保存的键名与实际使用的键名不一致，用户修改后无效：
     | 界面字段 | 修复前（无效键） | 修复后（实际生效） |
     |---|---|---|
     | 请求延迟 | `request_delay` | `request_delay_min` / `request_delay_max`（拆为两行输入） |
     | 重试次数 | `retry_count` | `max_retries` |
     | 超时时间 | `timeout`（默认值还是错的 30） | `request_timeout`（对齐实际默认 15） |
     | 历史记录条数 | 保存了但无处使用 | `_show_fund_history` 改为读取 `history_limit` |
   - 新增校验：`0 ≤ 最小请求延迟 ≤ 最大请求延迟`，不合法弹窗拦截。
   - 设置保存后立即生效（`anti_block.py` 每次请求均读取内存中的 `APP_SETTINGS`）。

3. **"更新选中"误清全表**（`gui/app.py`）
   - `_on_update_selected_success` 原先执行 `clear_funds()` 清空整表再全量重写，中途失败会导致本地快照全部丢失。
   - 现改为仅对本次更新的基金（合并 `is_custom` / `index_type` 后）执行 UPSERT 写入，不影响其他行。

### ✨ 功能改进：添加自选基金弹窗（`gui/dialogs.py` AddFundDialog）

1. **添加后不再自动关闭**
   - 原「关闭」按钮改为 **「完成」**：提交已加入的基金并关闭弹窗。
   - 原「添加选中」改为 **「添加选中（不关闭）」**：加入列表后弹窗保持打开。

2. **支持一次连续添加多只基金**
   - 可多选后批量加入，再搜索、再选、再加，全部累计到同一待提交列表。
   - 双击结果行 = 将该基金加入待提交列表（不再直接关闭弹窗）。

3. **防重复添加**
   - 内部以基金代码去重；重复添加时给出警示提示，不会重复计入。

4. **醒目的操作反馈**（替代原先不显眼的灰色小字）
   - 底部提示改为 **加粗 + 动态配色**：蓝色=常规提示、绿色=添加成功、橙色=重复/警示。
   - 已加入的基金在结果列表中显示绿色 **「✓ 基金名称」** 标识，重新搜索后自动恢复标记。
   - 「完成」按钮实时显示待提交数量，如 `完成 (3)`；列表为空时点「完成」会拦截提醒。
